# Fixtures

Збережені реальні артефакти (HTML/JSON/PDF) для тестів екстракції.
Правила: без персональних даних, без секретів; ім'я файлу — `<engine>__<domain>__<slug>.<ext>`
(наприклад `shopify__vendor.com__model-x.json`). Тести ніколи не ходять у мережу
(docs/09-testing-requirements.md §2).
