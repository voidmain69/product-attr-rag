"""Discovery — finding product URLs (docs/01, section 1).

Source priority: official feeds/APIs > sitemap.xml > category listings > manual seeds.
Emits canonicalized, deduplicated URLs into the fetch queue.
"""
